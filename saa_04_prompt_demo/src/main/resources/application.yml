server:
  port: 8084
  servlet:
    encoding:
      charset: UTF-8
      force: true
      enabled: true
spring:
  application:
    name: promptDemo
  ai:
    dashscope:
      agent:
        api-key: ${AI_DASHSCOPE_API_KEY}
      chat:
        options:
          model: qwen3.7-max